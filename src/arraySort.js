const functions = {
    arrSort(array) {
        array.sort();
        return array;
    },
    arrReverse(array) {
        array.reverse();
        return array;
    }
}

module.exports = functions;
